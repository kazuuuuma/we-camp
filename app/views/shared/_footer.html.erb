<nav class="navbar navbar-expand-lg navbar-light" style="background-color: #e3f2fd;">
  <div class="container-fluid">
    <p class="navbar-brand"><%= link_to "We CAMP", root_path %></p>
    <div class="collapse navbar-collapse" id="navbarCollapse">
      <ul class="navbar-nav">
        <li class="nav-item">
          <p class="nav-link" ><%= link_to "TOP", root_path %></p>
        </li>
        <% if user_signed_in? %>
        <li class="nav-item">
          <p class="nav-link"><%= link_to "マイページ", "/users/#{current_user.id}" %></p>
        </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>